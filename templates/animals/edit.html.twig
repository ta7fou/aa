{% extends 'baseBack.html.twig' %}
   {% block body %}
    <div class="container">
        <h1>Edit Animal</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-primary">Save Changes</button>
        {{ form_end(form) }}
   {% endblock %}

 